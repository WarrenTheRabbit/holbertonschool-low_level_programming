#include <stdio.h>

/**
 * main - Entry point
 * Uses printf to print a string.
 * Return: Always 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);

}
