/**
 * add - writes the character c to stdout
 * @a : addend
 * @b : augend
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int add(int a, int b)
{
	return (a + b);
}
